import styles from './Cart.css';

@CSSModules(styles)
export default class Cart extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div styleName="cart">
        cart dayo
      </div>
    );
  }
}
